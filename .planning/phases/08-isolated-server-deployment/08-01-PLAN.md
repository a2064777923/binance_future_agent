---
phase: "08-isolated-server-deployment"
plan: "08-01"
type: implementation
wave: 1
depends_on: []
files_modified:
  - deploy/systemd/binance-futures-agent.service
  - deploy/server-env.example
  - deploy/remote-bootstrap.sh
  - tests/test_deploy_assets.py
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
---

<objective>
Create reviewable server deployment manifests that are path-allowlisted and
secret-free.
</objective>

<tasks>
<task id="08-01-T1" type="code">
<read_first>
- .env.example
- src/bfa/config.py
- .planning/phases/08-isolated-server-deployment/08-CONTEXT.md
</read_first>
<action>
Add systemd unit template, server env example, and remote bootstrap shell script
for `/opt/binance-futures-agent` and `/etc/binance-futures-agent` only. The
bootstrap script must create app/data/log/runtime/raw export directories, a
placeholder env file with restrictive permissions, a Python venv, and install
the app without enabling live mode.
</action>
<acceptance_criteria>
- Static tests prove deployment assets contain no secret values.
- Static tests prove deployment assets avoid `F:\stock`, `/opt/stock`, unrelated
  systemd units, cron paths, and non-allowlisted app roots.
- Systemd unit references only the dedicated env file, app directory, venv, and
  runtime paths.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m unittest tests.test_deploy_assets`.
</verification>

<success_criteria>
- Server bootstrap assets are reviewable before execution.
- Generated paths are project-isolated.
- No secret material appears in deploy assets.
</success_criteria>
