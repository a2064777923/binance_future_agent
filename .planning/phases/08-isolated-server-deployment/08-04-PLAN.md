---
phase: "08-isolated-server-deployment"
plan: "08-04"
type: checkpoint
wave: 4
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - .planning/phases/08-isolated-server-deployment/08-04-SUMMARY.md
autonomous: false
requirements:
  - DEP-01
  - DEP-04
---

<objective>
Run or prepare the isolated server deployment smoke test without exposing
credentials.
</objective>

<tasks>
<task id="08-04-T1" type="checkpoint:human-action">
<read_first>
- scripts/deploy-server.ps1
- docs/deployment.md
- .planning/phases/08-isolated-server-deployment/08-VALIDATION.md
</read_first>
<action>
Run full local verification. If a secure non-interactive SSH path is available,
execute the deploy script in what-if mode, then apply mode, then run server
health checks. If not available, stop and present exact instructions for the
user to run the apply step without writing the password to files or command
history.
</action>
<acceptance_criteria>
- Local tests pass before any server mutation.
- Deployment command uses only the isolated server paths.
- Server smoke check output confirms dry-run config, DB access, runtime/log/data
  directories, and kill-switch path.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m unittest discover -s tests`, `git diff --check`, and server
health-check or checkpoint instructions.
</verification>

<success_criteria>
- Deployment is either safely completed in dry-run mode or blocked on a clear
  credential/auth checkpoint.
- No secret values are committed or written into generated artifacts.
- Existing server projects are not modified.
</success_criteria>
