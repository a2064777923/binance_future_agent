---
phase: "08-isolated-server-deployment"
plan: "08-02"
type: implementation
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/bfa/ops/__init__.py
  - src/bfa/ops/health.py
  - src/bfa/cli.py
  - tests/test_ops_health.py
  - tests/test_cli.py
autonomous: true
requirements:
  - DEP-04
---

<objective>
Add server-side health checks for config, directories, SQLite, kill switch,
Binance public connectivity, and OpenAI configuration.
</objective>

<tasks>
<task id="08-02-T1" type="code">
<read_first>
- src/bfa/config.py
- src/bfa/cli.py
- src/bfa/market/binance_rest.py
- src/bfa/ai/client.py
</read_first>
<action>
Implement `bfa.ops.health` and CLI `ops health-check` with JSON output. Checks
must be fakeable in tests and must redact config/secrets. Network checks should
be optional flags so local and CI tests avoid real Binance/OpenAI calls.
</action>
<acceptance_criteria>
- Unit tests cover passing dry-run health checks and failing missing directory or
  invalid live config checks.
- CLI tests prove JSON output includes check statuses without secret values.
- Tests use fake clients only.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m unittest tests.test_ops_health tests.test_cli`.
</verification>

<success_criteria>
- User can run a server-side health command after deployment.
- Health output is actionable and secret-safe.
- No real external calls are required by tests.
</success_criteria>
