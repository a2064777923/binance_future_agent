---
phase: "08-isolated-server-deployment"
plan: "08-03"
type: implementation
wave: 3
depends_on:
  - "08-01"
  - "08-02"
files_modified:
  - scripts/deploy-server.ps1
  - docs/deployment.md
  - tests/test_deploy_assets.py
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04
---

<objective>
Add a dry-run-first local deployment script and operator runbook.
</objective>

<tasks>
<task id="08-03-T1" type="code">
<read_first>
- deploy/remote-bootstrap.sh
- deploy/systemd/binance-futures-agent.service
- .planning/phases/08-isolated-server-deployment/08-CONTEXT.md
</read_first>
<action>
Add a PowerShell deployment script that packages git-tracked files, uploads the
archive to the configured host, runs the remote bootstrap script, installs the
systemd unit, and runs health checks. The script must default to what-if mode
and require explicit `-Apply` before remote mutation. Add a deployment runbook
that explains env file editing, dry-run service verification, and live-mode
activation as a later manual step.
</action>
<acceptance_criteria>
- Static tests prove the script defaults to what-if and has an explicit apply
  gate.
- The script contains no password, API key, or token values.
- Docs clearly state that first server mode is `dry_run` and live activation is
  a separate post-verification action.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m unittest tests.test_deploy_assets`.
</verification>

<success_criteria>
- User has a repeatable deployment path from Windows to the Linux server.
- Deployment defaults to safe preview.
- Operator docs preserve project isolation and secret hygiene.
</success_criteria>
