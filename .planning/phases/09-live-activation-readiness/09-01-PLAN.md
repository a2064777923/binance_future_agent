# Plan 09-01: Market-Heat Fallback And Candidate-Driven Live Evidence

## Objective

Produce candidate-bearing live cycles when Square exports and RSS feeds are
empty, while preserving fail-closed OpenAI behavior and live risk controls.

## Scope

- Add a conservative `market_heat` fallback narrative source derived from
  Binance USD-M public snapshots.
- Use it only when configured narrative sources return no records.
- Keep the source explicitly labelled as market-derived evidence, not social
  proof.
- Keep OpenAI as the slow-path decision/veto layer with timeout/backoff.
- Update Phase 9 planning state so it reflects the live timer and configured
  OpenAI key.

## Verification

- Unit tests cover market-heat record generation and weak-signal rejection.
- Agent tests prove empty narrative inputs can still produce one candidate and
  reach the AI decision path.
- Existing OpenAI backoff tests continue to prove no order intent is created
  when AI fails or is in cooldown.
- Full unit suite and `git diff --check` pass.
- Secret scan confirms provided credentials were not written to files.

## Server Follow-Up

After local verification, deploy the change to `/opt/binance-futures-agent`,
observe one live timer or direct service cycle, and capture whether it returns
`openai_backoff`, `ai_rejected`, `dry_run`/`submitted`, or another risk-gated
status without printing secrets.
