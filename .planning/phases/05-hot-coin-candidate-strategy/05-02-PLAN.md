---
phase: 05-hot-coin-candidate-strategy
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/bfa/cli.py
  - src/bfa/strategy/store.py
  - tests/test_strategy_store.py
  - tests/test_cli.py
autonomous: true
requirements:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
requirements_addressed:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
must_haves:
  truths:
    - "Generated candidate payloads can be persisted to the Phase 4 event store candidates category without schema changes (D-11, D-12)."
    - "CLI smoke commands can rank candidates from a replay JSON file and optionally write candidate payloads to SQLite."
    - "Phase 5 final verification proves no OpenAI, order placement, private Binance account state, live source collection, deployment, or F:\\stock access was added."
---

<objective>
Wire candidate generation into event-store persistence and CLI smoke commands.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist candidate signals</name>
  <files>src/bfa/strategy/store.py, tests/test_strategy_store.py</files>
  <behavior>
    - Test 1: Candidate signals insert into event-store `candidates` category.
    - Test 2: Payloads include score, reasons, data-quality notes, and features.
  </behavior>
  <action>Add helper to persist candidates through `EventStore.insert_artifact`.</action>
  <verify>
    <automated>python -m unittest tests.test_strategy_store -v</automated>
  </verify>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add candidate CLI smoke command and verification</name>
  <files>src/bfa/cli.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: `strategy candidates --replay <file>` prints ranked candidates and rejected records.
    - Test 2: `--db <tmp>` persists candidates to SQLite.
    - Test 3: Existing CLI commands still pass.
  </behavior>
  <action>Add a `strategy candidates` subcommand. It reads a replay packet JSON file and uses deterministic defaults.</action>
  <verify>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
</task>
</tasks>

<success_criteria>
Phase 5 is complete when candidate generation works from replay data, outputs ranked/rejected records with audit fields, and can persist candidate payloads.
</success_criteria>

<output>
Create `.planning/phases/05-hot-coin-candidate-strategy/05-02-SUMMARY.md` and `05-VERIFICATION.md` when done.
</output>

