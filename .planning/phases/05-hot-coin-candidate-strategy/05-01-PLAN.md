---
phase: 05-hot-coin-candidate-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bfa/strategy/__init__.py
  - src/bfa/strategy/candidates.py
  - src/bfa/strategy/features.py
  - tests/fixtures/strategy/replay_packet.json
  - tests/test_strategy_features.py
  - tests/test_strategy_candidates.py
autonomous: true
requirements:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
requirements_addressed:
  - STR-01
  - STR-02
  - STR-03
  - STR-04
must_haves:
  truths:
    - "Candidate generation combines narrative heat and market anomaly features into ranked deterministic candidates (STR-01, D-03, D-04, D-09, D-10)."
    - "Each candidate includes reason codes, data-quality notes, source event IDs, market event IDs, and raw features (STR-02, D-01)."
    - "Missing features reduce score/confidence and add data-quality notes instead of crashing candidate generation (D-05)."
    - "Rejected symbols include explicit rejection reason codes and notes instead of silent dropping (STR-03, D-02, D-06)."
    - "Default strategy config is conservative for the 100 USDT pilot: controlled symbols only, liquidity threshold, freshness limits, and top-N output (D-07)."
    - "Candidate generation is deterministic from replay packets and does not call OpenAI, Binance private APIs, source collectors, wall-clock time, or order endpoints (STR-04, D-08, D-10)."
---

<objective>
Implement deterministic hot-coin candidate feature extraction, scoring, ranking, and rejection over replay packets.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add candidate models and feature extraction</name>
  <files>src/bfa/strategy/__init__.py, src/bfa/strategy/candidates.py, src/bfa/strategy/features.py, tests/fixtures/strategy/replay_packet.json, tests/test_strategy_features.py</files>
  <behavior>
    - Test 1: Replay narrative and market events aggregate into per-symbol narrative and market features.
    - Test 2: Missing features add quality notes rather than crashing.
    - Test 3: Event IDs are preserved for auditability.
  </behavior>
  <action>Create dataclasses for strategy config, feature bundle, candidate signal, and rejected candidate. Parse Phase 4 replay packet records only.</action>
  <verify>
    <automated>python -m unittest tests.test_strategy_features -v</automated>
  </verify>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add deterministic candidate scorer and rejection gates</name>
  <files>src/bfa/strategy/candidates.py, tests/test_strategy_candidates.py</files>
  <behavior>
    - Test 1: Strong narrative plus confirmed market momentum ranks above weaker symbols.
    - Test 2: Stale, low-liquidity, excessive-volatility, missing-market, and unconfigured symbols are rejected with reasons.
    - Test 3: Running generation twice on identical replay packets returns identical output.
  </behavior>
  <action>Implement `generate_candidates(replay_packet, config)` returning ranked candidates and rejected records. Keep scoring pure and deterministic.</action>
  <verify>
    <automated>python -m unittest tests.test_strategy_features tests.test_strategy_candidates -v</automated>
  </verify>
</task>
</tasks>

<success_criteria>
STR-01 through STR-04 are covered at pure strategy level.
</success_criteria>

<output>
Create `.planning/phases/05-hot-coin-candidate-strategy/05-01-SUMMARY.md` when done.
</output>
