---
phase: 04-event-store-and-replay-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/bfa/cli.py
  - src/bfa/event_store/replay.py
  - src/bfa/event_store/report.py
  - tests/test_event_store_replay_report.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVT-01
  - EVT-02
  - EVT-03
requirements_addressed:
  - EVT-01
  - EVT-02
  - EVT-03
must_haves:
  truths:
    - "Replay packets expose deterministic historical windows for Phase 5 candidate generation without implementing scoring in Phase 4 (EVT-02, D-07, D-09)."
    - "Review reports compute trade count, win rate, gross/net PnL, fees, slippage, expectancy, max drawdown, and reason-code performance from stored outcomes/fills (EVT-03, D-10, D-11)."
    - "CLI smoke commands can initialize the DB and print review reports without secrets, network calls, OpenAI, Binance private APIs, or live orders."
---

<objective>
Add replay packet and review report utilities plus CLI smoke commands for the event-store layer.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement replay and review reporting</name>
  <files>src/bfa/event_store/replay.py, src/bfa/event_store/report.py, tests/test_event_store_replay_report.py</files>
  <behavior>
    - Test 1: Replay packets are stable and include event count, start/end, symbols, and records.
    - Test 2: Empty review reports return zero metrics.
    - Test 3: Synthetic outcomes/fills compute win rate, expectancy, drawdown, fees, slippage, and reason-code metrics.
  </behavior>
  <action>Build pure Python replay/report functions over `EventStore`; keep them offline and deterministic.</action>
  <verify>
    <automated>python -m unittest tests.test_event_store_replay_report -v</automated>
  </verify>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add event-store CLI smoke commands and final verification</name>
  <files>src/bfa/cli.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: `event-store init --db <tmp>` creates the schema and prints JSON.
    - Test 2: `event-store report --db <tmp>` prints review metrics JSON.
    - Test 3: Existing CLI commands still pass.
  </behavior>
  <action>Add `event-store init` and `event-store report` subcommands. Use `BFA_DB_PATH` when `--db` is omitted.</action>
  <verify>
    <automated>python -m unittest discover -s tests</automated>
  </verify>
</task>
</tasks>

<success_criteria>
EVT-01 through EVT-03 are covered by migrations, repository helpers, deterministic replay packets, review metrics, and CLI smoke commands.
</success_criteria>

<output>
Create `.planning/phases/04-event-store-and-replay-foundation/04-03-SUMMARY.md` and `04-VERIFICATION.md` when done.
</output>

