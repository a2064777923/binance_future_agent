---
phase: 04-event-store-and-replay-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/bfa/event_store/models.py
  - src/bfa/event_store/store.py
  - tests/test_event_store_repository.py
autonomous: true
requirements:
  - EVT-01
  - EVT-02
requirements_addressed:
  - EVT-01
  - EVT-02
must_haves:
  truths:
    - "The store can persist Phase 3 narrative records and Phase 2 market snapshots with JSON payloads and replay event rows (EVT-01, D-06)."
    - "Future artifact categories can be inserted as generic JSON records without adding scoring, AI, order execution, or exchange calls (D-03, D-06, D-09)."
    - "Historical windows can be queried deterministically by time and optional symbol (EVT-02, D-07, D-08)."
---

<objective>
Add repository helpers for writing normalized records and reading deterministic event windows.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement event-store repository</name>
  <files>src/bfa/event_store/models.py, src/bfa/event_store/store.py, tests/test_event_store_repository.py</files>
  <behavior>
    - Test 1: Narrative and market snapshot inserts create typed rows plus `events` rows.
    - Test 2: Generic category inserts work for candidates, AI decisions, order intents, exchange responses, fills, risk state, and outcomes.
    - Test 3: Replay event queries are stable and filter by time/symbol.
  </behavior>
  <action>Create `EventStore` with insert helpers, JSON serialization, and `events_between(start, end, symbol=None)`.</action>
  <verify>
    <automated>python -m unittest tests.test_event_store_repository -v</automated>
  </verify>
</task>
</tasks>

<success_criteria>
Stored inputs and future artifacts can be persisted and queried in deterministic replay order.
</success_criteria>

<output>
Create `.planning/phases/04-event-store-and-replay-foundation/04-02-SUMMARY.md` when done.
</output>

