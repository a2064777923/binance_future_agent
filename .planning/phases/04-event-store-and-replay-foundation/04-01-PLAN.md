---
phase: 04-event-store-and-replay-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bfa/event_store/__init__.py
  - src/bfa/event_store/migrations.py
  - tests/test_event_store_migrations.py
autonomous: true
requirements:
  - EVT-01
requirements_addressed:
  - EVT-01
must_haves:
  truths:
    - "SQLite migrations create tables for narratives, market snapshots, candidates, AI decisions, orders, fills, risk state, outcomes, and generic replay events (EVT-01, D-04, D-05)."
    - "Migrations are idempotent and dependency-free using Python sqlite3 (D-01, D-02)."
    - "Event-store tests use temporary databases and do not touch runtime DB files, secrets, network, or F:\\stock (D-12, D-13)."
---

<objective>
Create the SQLite event-store schema and idempotent migration helper.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement event-store migrations</name>
  <files>src/bfa/event_store/__init__.py, src/bfa/event_store/migrations.py, tests/test_event_store_migrations.py</files>
  <behavior>
    - Test 1: Running migrations creates all EVT-01 category tables plus `events` and `schema_version`.
    - Test 2: Running migrations twice is safe and preserves schema version.
    - Test 3: Core indexes exist for replay queries.
  </behavior>
  <action>Implement `connect(path)` and `migrate(connection)` helpers using sqlite3. Keep SQL idempotent and standard-library only.</action>
  <verify>
    <automated>python -m unittest tests.test_event_store_migrations -v</automated>
  </verify>
</task>
</tasks>

<success_criteria>
EVT-01 schema foundation exists and can be created repeatably in temporary SQLite databases.
</success_criteria>

<output>
Create `.planning/phases/04-event-store-and-replay-foundation/04-01-SUMMARY.md` when done.
</output>

