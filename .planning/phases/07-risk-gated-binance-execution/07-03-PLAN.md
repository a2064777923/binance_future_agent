---
phase: "07-risk-gated-binance-execution"
plan: "07-03"
type: implementation
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/bfa/execution/executor.py
  - src/bfa/execution/store.py
  - src/bfa/cli.py
  - README.md
  - tests/test_execution_executor.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EXE-01
  - EXE-02
  - EXE-03
  - EXE-04
---

<objective>
Wire dry-run and explicit live execution through persistence and CLI.
</objective>

<tasks>
<task id="07-03-T1" type="code">
<read_first>
- src/bfa/event_store/store.py
- src/bfa/ai/schema.py
- src/bfa/execution/risk.py
</read_first>
<action>
Implement `ExecutionEngine` that validates risk, persists `order_intents`, and
in live mode sets isolated margin/leverage before `POST /fapi/v1/order`.
</action>
<acceptance_criteria>
- Dry-run test persists an order intent and fake Binance transport has zero calls.
- Live rejection test persists rejected intent and does not call new order.
- Accepted live test calls margin, leverage, and new order in order using fake client.
</acceptance_criteria>
</task>

<task id="07-03-T2" type="code">
<read_first>
- src/bfa/cli.py
- tests/test_cli.py
</read_first>
<action>
Add `execution run --decision <json> --mark-price <price> --decided-at <time>`
with optional `--exchange-info`, `--db`, and `--env-file`.
</action>
<acceptance_criteria>
- CLI dry-run output includes `submitted: false`, `status`, `intent`, and
  `persisted`.
- CLI live missing-precondition path exits nonzero and does not call fake client.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m unittest tests.test_execution_executor tests.test_cli`.
</verification>

<success_criteria>
- Dry-run mode creates order intents without submitting orders.
- Live mode is explicit and risk-gated.
- Exchange responses are persisted when live fake client returns.
</success_criteria>

## Artifacts this phase produces

- `bfa.execution.executor.ExecutionEngine`
- `bfa.execution.store.persist_order_intent`
- `bfa.execution.store.persist_exchange_response`
- `bfa.cli` `execution run` command
