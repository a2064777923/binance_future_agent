---
phase: 03-narrative-and-hot-coin-collection-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - .env.example
  - src/bfa/config.py
  - src/bfa/cli.py
  - src/bfa/narrative/dedup.py
  - src/bfa/narrative/jsonl_writer.py
  - src/bfa/narrative/collector.py
  - tests/test_config.py
  - tests/test_cli.py
  - tests/test_narrative_dedup.py
  - tests/test_narrative_collector.py
autonomous: true
requirements:
  - NAR-01
  - NAR-02
  - NAR-03
  - NAR-04
requirements_addressed:
  - NAR-01
  - NAR-02
  - NAR-03
  - NAR-04
must_haves:
  truths:
    - "Duplicate narrative records collapse by source ID or deterministic fingerprint before scoring (NAR-04, D-10, D-11)."
    - "Narrative JSONL output writes normalized records under caller-managed runtime/data paths without introducing durable SQLite storage (D-12)."
    - "CLI smoke commands can run manual/export and RSS collection through fake/offline inputs and print secret-safe JSON summaries (NAR-01, NAR-02)."
    - "Final Phase 3 verification proves no scoring, OpenAI calls, Binance private APIs, orders, SQLite event store, deployment, secret-file reads, or F:\\stock access were added (D-13, D-14, D-15)."
  artifacts:
    - path: "src/bfa/narrative/dedup.py"
      provides: "Deterministic narrative deduplication"
      contains: "deduplicate_records"
    - path: "src/bfa/narrative/jsonl_writer.py"
      provides: "Narrative JSONL output writer"
      contains: "write_jsonl_records"
    - path: "src/bfa/narrative/collector.py"
      provides: "Composite narrative collection orchestration"
      contains: "NarrativeCollectionRunner"
    - path: "src/bfa/cli.py"
      provides: "Narrative CLI smoke commands"
      contains: "narrative"
  key_links:
    - from: "src/bfa/cli.py"
      to: "src/bfa/narrative/collector.py"
      via: "CLI builds a collection runner from config and writes normalized JSONL records"
      pattern: "NarrativeCollectionRunner"
    - from: "src/bfa/narrative/collector.py"
      to: "src/bfa/narrative/dedup.py"
      via: "runner deduplicates adapter records before returning/writing them"
      pattern: "deduplicate_records"
---

<objective>
Finish Phase 3 by adding deterministic deduplication, narrative JSONL output, config wiring, and CLI smoke commands.

Purpose: NAR-01 through NAR-04 require source records to become deduplicated normalized JSONL evidence that later event-store, strategy, and AI phases can consume.
Output: Deduplication utilities, JSONL writer, composite collection runner, config helpers, CLI command group, integration tests, summaries, and final boundary checks.
</objective>

<execution_context>
@C:/Users/KingHong/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/KingHong/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-narrative-and-hot-coin-collection-layer/03-CONTEXT.md
@.planning/phases/03-narrative-and-hot-coin-collection-layer/03-RESEARCH.md
@.planning/phases/03-narrative-and-hot-coin-collection-layer/03-02-SUMMARY.md
@.env.example
@src/bfa/config.py
@src/bfa/cli.py
@src/bfa/market/snapshot_writer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic deduplication</name>
  <files>src/bfa/narrative/dedup.py, tests/test_narrative_dedup.py</files>
  <behavior>
    - Test 1: Records with the same `(source, source_id)` collapse to one record.
    - Test 2: Records without source IDs collapse by deterministic fingerprint over source, author, URL, text, symbols, and timestamp bucket.
    - Test 3: Non-duplicates preserve first-seen order and symbol mentions remain deduplicated.
  </behavior>
  <action>Implement `dedup_key(record)` and `deduplicate_records(records)`. Use stable hashing for fingerprint keys. Do not add fuzzy matching, semantic clustering, AI calls, or SQLite state.</action>
  <verify>
    <automated>python -m unittest tests.test_narrative_dedup -v</automated>
  </verify>
  <done>Duplicate narrative records collapse deterministically before later scoring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add collection runner and JSONL writer</name>
  <files>src/bfa/narrative/collector.py, src/bfa/narrative/jsonl_writer.py, tests/test_narrative_collector.py</files>
  <behavior>
    - Test 1: A runner combines manual/export and RSS adapter records, deduplicates them, and returns normalized records.
    - Test 2: JSONL writer writes one normalized record per line under caller-provided output paths.
    - Test 3: Runner works with injected fake adapters and does not require live network, cookies, tokens, OpenAI, Binance credentials, or SQLite.
  </behavior>
  <action>Create a small `NarrativeCollectionRunner` that accepts adapter instances, calls `collect()`, deduplicates results, and optionally writes JSONL through `write_jsonl_records`. Mirror the market snapshot writer style while keeping this package independent.</action>
  <verify>
    <automated>python -m unittest tests.test_narrative_collector tests.test_narrative_dedup -v</automated>
  </verify>
  <done>Source adapters can be composed into deduplicated JSONL evidence batches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire config and narrative CLI smoke commands</name>
  <files>.env.example, src/bfa/config.py, src/bfa/cli.py, tests/test_config.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: Config helpers parse `RSS_FEED_URLS` and `TELEGRAM_CHANNELS` as comma-separated lists without requiring credentials in dry-run mode.
    - Test 2: `python -m bfa.cli narrative collect --env-file .env.example --output <tmp.jsonl>` works in tests with injected fake adapters/runner and prints JSON summary.
    - Test 3: CLI does not print secret values and does not read `SQUARE_COOKIE_FILE`, `X_BEARER_TOKEN`, or `TELEGRAM_BOT_TOKEN`.
  </behavior>
  <action>Add minimal config helper(s) for narrative feed/channel lists if needed. Extend the argparse CLI with a `narrative collect` command that can build manual/export and RSS collectors from config in real use, while tests inject a fake runner or collector factory. Keep all output JSON and secret-safe.</action>
  <verify>
    <automated>python -m unittest tests.test_config tests.test_cli -v</automated>
  </verify>
  <done>Users have a CLI smoke path for Phase 3 narrative collection and tests prove it is offline and secret-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multiple adapters -> runner | Different sources can emit duplicate or malformed normalized records. |
| CLI args -> output writer | User controls output path and env file. |
| Config -> source collectors | Source URLs and export directories configure future collection behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | deduplication | mitigate | Use stable source-id keys and deterministic fingerprints; preserve first-seen records. |
| T-03-03-02 | Information Disclosure | CLI/config | mitigate | Print JSON summaries only, reuse existing config loading, and do not read cookie/token/key files. |
| T-03-03-03 | Denial of Service | runner | mitigate | Keep Phase 3 runner one-shot and caller-driven; no scheduler or broad crawling. |
| T-03-03-04 | Elevation of Privilege | phase boundary | mitigate | Final greps prove no OpenAI, private Binance account/order, SQLite, deployment, or stock-project access was introduced. |
</threat_model>

<verification>
Run final Phase 3 verification:
- `python -m unittest discover -s tests`
- `git diff --check`
- `git grep -n "F:\\\\stock" -- . ":(exclude).planning/**"` should only match allowed repository guidance if any; Phase 3 source/tests must not introduce stock-project reads or writes.
- `git grep -nE "openai|OPENAI|account|order|listenKey|userData|sqlite|sqlite3|systemctl|ssh|scp" -- src tests` should show no Phase 3 implementation of AI, private Binance execution, durable event store, or deployment.
- `git grep -nE "SQUARE_COOKIE_FILE|X_BEARER_TOKEN|TELEGRAM_BOT_TOKEN|API_KEY|API_SECRET|SECRET|TOKEN|COOKIE" -- src tests .env.example` should show only config/redaction placeholders and synthetic tests; no code reads secret files or commits values.
</verification>

<success_criteria>
Phase 3 is complete when NAR-01 through NAR-04 are all covered: Binance Square has a manual/export ingestion path, fallback RSS/Atom ingestion exists, all records normalize to the shared narrative shape, duplicates collapse before scoring, JSONL evidence can be written, and CLI smoke tests prove the layer works offline and secret-free.
</success_criteria>

<output>
Create `.planning/phases/03-narrative-and-hot-coin-collection-layer/03-03-SUMMARY.md` and `.planning/phases/03-narrative-and-hot-coin-collection-layer/03-VERIFICATION.md` when done.
</output>

