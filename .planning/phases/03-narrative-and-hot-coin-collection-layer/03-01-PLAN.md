---
phase: 03-narrative-and-hot-coin-collection-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bfa/narrative/__init__.py
  - src/bfa/narrative/models.py
  - src/bfa/narrative/symbols.py
  - tests/fixtures/narrative/manual_records.json
  - tests/test_narrative_models.py
  - tests/test_narrative_symbols.py
autonomous: true
requirements:
  - NAR-03
requirements_addressed:
  - NAR-03
must_haves:
  truths:
    - "Narrative records normalize source, source_id, author, symbol_mentions, text, url, published_at, collected_at, engagement, raw, and quality_flags (NAR-03, D-07)."
    - "Engagement is optional and normalized as a dictionary when present; missing engagement adds no blocker and is reflected through quality flags only when useful (D-09)."
    - "Symbol extraction is conservative, futures-aware, order-preserving, and flags ambiguous or missing mentions rather than inventing trades (D-05, D-08)."
    - "The record model preserves source and author fields for later reliability analysis without blessing or hardcoding any public poster as reliable (D-06)."
    - "Narrative code lives under src/bfa/narrative and does not call OpenAI, Binance private APIs, account/order endpoints, SQLite, deployment commands, or F:\\stock (D-15)."
  artifacts:
    - path: "src/bfa/narrative/models.py"
      provides: "Normalized narrative record dataclass and source record helpers"
      contains: "NormalizedNarrativeRecord"
    - path: "src/bfa/narrative/symbols.py"
      provides: "Conservative crypto/futures symbol extraction"
      contains: "extract_symbol_mentions"
    - path: "tests/test_narrative_symbols.py"
      provides: "Offline coverage for explicit pairs, cashtags, bare allowlisted bases, duplicates, and ambiguity flags"
  key_links:
    - from: "src/bfa/narrative/models.py"
      to: "src/bfa/narrative/symbols.py"
      via: "normalization uses symbol extraction to populate symbol_mentions and quality_flags"
      pattern: "extract_symbol_mentions"
---

<objective>
Create the Phase 3 narrative package foundation: typed normalized narrative records and conservative futures-aware symbol extraction.

Purpose: NAR-03 and decisions D-05, D-07, D-08, and D-15 require an auditable record shape that later source adapters, deduplication, candidate scoring, and AI prompts can share without guessing symbol intent.
Output: `bfa.narrative` package, normalized record model, symbol extraction helper, static fixtures, and unit tests.
</objective>

<execution_context>
@C:/Users/KingHong/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/KingHong/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-narrative-and-hot-coin-collection-layer/03-CONTEXT.md
@.planning/phases/03-narrative-and-hot-coin-collection-layer/03-RESEARCH.md
@.planning/phases/02-binance-futures-market-data-layer/02-VERIFICATION.md
@src/bfa/config.py
@src/bfa/market/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized narrative record model</name>
  <files>src/bfa/narrative/__init__.py, src/bfa/narrative/models.py, tests/test_narrative_models.py</files>
  <behavior>
    - Test 1: `NormalizedNarrativeRecord.to_dict()` returns JSON-serializable fields for source, source_id, author, symbol_mentions, text, url, published_at, collected_at, engagement, raw, and quality_flags.
    - Test 2: Normalization trims strings, collapses duplicate symbol mentions in order, and preserves raw/source context.
    - Test 3: Missing text or missing source is rejected with `ValueError`; missing optional fields such as author, published_at, and engagement produce quality flags or empty dictionaries rather than crashes.
  </behavior>
  <action>Create `src/bfa/narrative` and define `NormalizedNarrativeRecord`. Include a helper such as `normalize_narrative_record(raw, default_source, known_symbols=None, collected_at=None)` that normalizes common manual/export fields into the dataclass. Keep values JSON-serializable and dependency-free.</action>
  <verify>
    <automated>python -m unittest tests.test_narrative_models -v</automated>
  </verify>
  <done>Narrative records have the stable Phase 3 shape and quality flag behavior required by downstream adapters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement conservative symbol extraction</name>
  <files>src/bfa/narrative/symbols.py, tests/fixtures/narrative/manual_records.json, tests/test_narrative_symbols.py</files>
  <behavior>
    - Test 1: Text containing `ESPORTSUSDT`, `BTC/USDT`, `BTC-USDT`, and `$SOL` extracts normalized USDT symbols.
    - Test 2: Bare bases such as `ETH` only map to `ETHUSDT` when the mapped symbol is in a supplied known-symbol allowlist.
    - Test 3: Duplicate mentions collapse in first-seen order and ambiguous uppercase tokens are reflected in quality flags.
  </behavior>
  <action>Implement `extract_symbol_mentions(text, known_symbols=None, quote_assets=("USDT",))` returning a small result object or tuple with `symbols` and `quality_flags`. Avoid fuzzy matching and do not infer trade direction, leverage, entry, or order intent.</action>
  <verify>
    <automated>python -m unittest tests.test_narrative_symbols tests.test_narrative_models -v</automated>
  </verify>
  <done>Symbol extraction supports the screenshot-style hot coin evidence while remaining conservative enough for future live futures use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual/export text -> symbol extractor | User-copied source text can contain noisy uppercase words, URLs, punctuation, and misleading tickers. |
| Raw source record -> normalized record | External fields enter the auditable narrative shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | symbol extraction | mitigate | Map only explicit pairs/cashtags and allowlisted bare bases; add ambiguity flags instead of silently trading unknown tokens. |
| T-03-01-02 | Information Disclosure | raw narrative model | mitigate | Store raw source context only from caller-provided records; do not read cookie/token/key files or secrets. |
| T-03-01-03 | Elevation of Privilege | narrative package | mitigate | Keep this plan data-only with no OpenAI, Binance private API, account/order, SQLite, deployment, or server access. |
</threat_model>

<verification>
Run:
- `python -m unittest tests.test_narrative_models tests.test_narrative_symbols -v`
- `git diff --check`
</verification>

<success_criteria>
NAR-03 is satisfied at the foundation level: downstream code can create JSON-serializable narrative records with auditable fields, extracted symbols, engagement, timestamps, raw context, and quality flags.
</success_criteria>

<output>
Create `.planning/phases/03-narrative-and-hot-coin-collection-layer/03-01-SUMMARY.md` when done.
</output>
