---
phase: "06-openai-decision-layer"
plan: "06-03"
type: implementation
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/bfa/cli.py
  - .env.example
  - README.md
  - tests/test_cli.py
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
---

<objective>
Expose the OpenAI decision layer through CLI smoke commands and complete
phase-level verification.
</objective>

<tasks>
<task id="06-03-T1" type="code">
<read_first>
- src/bfa/cli.py
- tests/test_cli.py
</read_first>
<action>
Add `bfa ai decide --candidate <json> --decided-at <timestamp>` with optional
`--journal`, `--db`, and `--env-file` flags. Use the existing dependency
injection style for tests.
</action>
<acceptance_criteria>
- CLI prints JSON containing `accepted`, `decision`, `validation_errors`,
  `journaled`, and `persisted`.
- Fake-client CLI test passes without live OpenAI calls.
</acceptance_criteria>
</task>

<task id="06-03-T2" type="docs">
<read_first>
- .env.example
- README.md
</read_first>
<action>
Document Phase 6 OpenAI decision command and keep secrets as blank env values.
</action>
<acceptance_criteria>
- `.env.example` contains only variable names/defaults, no secret values.
- README states Phase 6 does not place orders.
</acceptance_criteria>
</task>
</tasks>

<verification>
Run `python -m unittest discover -s tests` and `git diff --check`.
</verification>

<success_criteria>
- AI-01 through AI-04 are reachable through local code and CLI.
- Phase 6 has no order placement or server deployment behavior.
- All tests pass with fake transports.
</success_criteria>

## Artifacts this phase produces

- `bfa.cli` `ai decide` command
- `main(..., ai_client_factory=...)`
- README Phase 6 usage snippet
