# Plan 44-01: Forward-Paper Guarded Setup Calibration

## Goal

Turn Phase 43 attribution into a stricter paper/backtest setup variant that can
be evaluated before any live resume.

## Scope

- Extend setup profiles with explicit side disabling.
- Extend setup profiles with explicit symbol exclusions.
- Add a built-in guarded quant setup variant for backtests and forward-paper
  observation.
- Ensure guarded rejections are visible in setup reasons.
- Add tests proving default/live profile behavior is unchanged unless guards
  are configured.
- Keep the change paper/backtest-first; no live timer restore and no risk
  profile apply.

## Out Of Scope

- No live timer restore.
- No risk profile apply.
- No exchange order, close, or position adjustment.
- No automatic claim that guarded paper evidence is profitable.
- No automatic paper timer switch until server verification passes.

## Verification

- Focused strategy setup and backtest variant tests.
- CLI/backtest or matrix smoke for the guarded variant.
- Full local test suite.
- `git diff --check`.
- Secret-pattern scan over the working diff.
- Server deployment and guarded variant verification before any paper timer
  change.
