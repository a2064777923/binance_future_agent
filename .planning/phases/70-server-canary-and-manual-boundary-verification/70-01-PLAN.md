---
phase: 70
plan: 01
title: Server Canary And Manual Boundary Verification
status: complete
requirements_addressed: [OPS-04, OPS-05, RISK-05]
completed: 2026-06-21
---

# Plan 70-01: Server Canary And Manual Boundary Verification

## Objective

Deploy the completed v1.27 local changes to the isolated server, verify the
live/paper timers and one-shot service state, produce current server evidence,
and prove `BTWUSDT` remains manual-only under the widened live pilot caps.

## Must Haves

- Deploy only under `/opt/binance-futures-agent` and
  `/etc/binance-futures-agent`.
- Preserve server secrets and avoid printing raw API keys or passwords.
- Restore live and paper timers after deployment.
- Run server focused and full tests after deployment.
- Generate server canary artifacts for live status, exposure capacity,
  position review, pilot learning packet, and live-cycle explainability.
- Run one live service canary cycle with timers paused so it cannot overlap a
  scheduled run.
- Verify manual `BTWUSDT` is visible as manual exposure and excluded from
  bot-managed count and bot exit/action recommendations.
- Scan Phase 70 server artifacts for sensitive fields.

## Verification

- Server focused tests:
  `/opt/binance-futures-agent/.venv/bin/python -m unittest tests.test_execution_sizing tests.test_execution_risk tests.test_agent_runner tests.test_ops_exposure_status tests.test_ops_risk_profile tests.test_ops_pilot_learning_packet tests.test_cli -v`
- Server full tests:
  `/opt/binance-futures-agent/.venv/bin/python -m unittest discover -s tests`
- Server health:
  `/opt/binance-futures-agent/.venv/bin/python -m bfa.cli ops health-check --env-file /etc/binance-futures-agent/env --db /opt/binance-futures-agent/data/agent.sqlite --skip-network`
- Server live one-shot:
  `systemctl start binance-futures-agent-live.service`
- Final server state:
  live timer active, live service inactive, paper timer active, paper service
  inactive.
