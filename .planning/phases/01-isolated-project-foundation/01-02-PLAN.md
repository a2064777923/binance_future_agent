---
phase: 01-isolated-project-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/bfa/redaction.py
  - tests/test_redaction.py
autonomous: true
requirements:
  - CFG-03
requirements_addressed:
  - CFG-03
must_haves:
  truths:
    - "Secret-shaped values are removed from flat and nested diagnostics before display per D-05."
    - "Sensitive keys are detected by name even when future config diagnostics become nested."
    - "Tests prove original sensitive values are absent from redacted output."
  artifacts:
    - path: "src/bfa/redaction.py"
      provides: "Shared secret redaction helper"
      exports:
        - "is_sensitive_key"
        - "redact_value"
        - "redact_object"
    - path: "tests/test_redaction.py"
      provides: "Unit tests for key detection, scalar redaction, recursive redaction, and exact-value absence"
  key_links:
    - from: "src/bfa/redaction.py"
      to: "src/bfa/config.py"
      via: "Plan 03 imports the shared redaction helper for config summaries"
      pattern: "from bfa.redaction import"
    - from: "src/bfa/redaction.py"
      to: "src/bfa/cli.py"
      via: "Plan 04 prints only redacted diagnostics"
      pattern: "redact_object"
---

<objective>
Create the shared redaction utility and tests that prevent secrets from appearing in logs, diagnostics, config-check output, or error messages.

Purpose: Satisfy CFG-03 and D-05 before config loading and CLI diagnostics are implemented.
Output: Importable redaction helpers plus unittest coverage for flat, nested, and list-containing structures.
</objective>

<source_audit>
GOAL covered: Establish secret hygiene.
REQ covered: CFG-03.
RESEARCH covered: Secret field names, recursive redaction, exact-value absence tests, and CLI-safe redacted summaries.
CONTEXT covered: D-05 shared redaction helper before display or logging.
Not covered in this plan: Config mode validation and config-check CLI are assigned to plans 03 and 04.
</source_audit>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Write redaction behavior tests</name>
  <files>tests/test_redaction.py</files>
  <action>Create unittest cases before implementation for CFG-03. Cover sensitive key names containing secret, token, password, cookie, and API key concepts; empty values staying empty; short non-empty sensitive values becoming a redaction marker; longer sensitive values never exposing the full original; non-sensitive values remaining readable; and recursive redaction through dictionaries and lists. Use synthetic test strings only, with no real credentials and no copied credential-shaped values.</action>
  <verify>
    <automated>python -m pip install -e . && python -m unittest tests.test_redaction -v</automated>
  </verify>
  <acceptance_criteria>The test suite fails until `src/bfa/redaction.py` exists, and every test asserts that original sensitive input strings are absent from serialized redacted output.</acceptance_criteria>
  <done>The test suite fails until `src/bfa/redaction.py` exists, and every test asserts that original sensitive input strings are absent from serialized redacted output.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared redaction helper</name>
  <files>src/bfa/redaction.py</files>
  <action>Implement the shared helper per D-05 with `is_sensitive_key(key: str) -> bool`, `redact_value(value: object) -> object`, and `redact_object(value: object) -> object`. Treat keys containing sensitive concepts case-insensitively. Redact recursively for dictionaries, lists, tuples, and scalar leaf values while preserving enough structure for diagnostics to be useful. Avoid logging, printing, reading env files, or handling real secrets in this module.</action>
  <verify>
    <automated>python -m pip install -e . && python -m unittest tests.test_redaction -v</automated>
  </verify>
  <acceptance_criteria>All redaction tests pass, recursive structures keep their shape, and no raw sensitive test value appears in redacted structures or serialized diagnostic output.</acceptance_criteria>
  <done>All redaction tests pass, recursive structures keep their shape, and no raw sensitive test value appears in redacted structures or serialized diagnostic output.</done>
</task>

<task type="auto">
  <name>Task 3: Add redaction hygiene verification</name>
  <files>tests/test_redaction.py</files>
  <action>Add one regression test that serializes a nested diagnostic payload after redaction and checks exact sensitive input strings are absent. Keep this as a behavior test for D-05 so later config and CLI code can rely on the same helper instead of duplicating redaction rules.</action>
  <verify>
    <automated>python -m pip install -e . && python -m unittest tests.test_redaction -v && git diff --check</automated>
  </verify>
  <acceptance_criteria>The redaction module has focused tests that can be run independently and the diff has no whitespace errors.</acceptance_criteria>
  <done>The redaction module has focused tests that can be run independently and the diff has no whitespace errors.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config values -> diagnostics | Secret values become printable diagnostics only after redaction. |
| nested runtime objects -> logs | Nested structures may contain sensitive fields introduced by later phases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | `src/bfa/redaction.py` | mitigate | Centralize sensitive key detection and recursive value redaction per D-05. |
| T-01-05 | Information Disclosure | `tests/test_redaction.py` | mitigate | Use synthetic inputs and assert exact input strings are absent from serialized outputs. |
| T-01-SC | Tampering | package installs | accept | This plan adds no external packages. |
</threat_model>

<verification>
Run `python -m pip install -e .`, `python -m unittest tests.test_redaction -v`, and `git diff --check`. If tests are run as part of the whole phase, also run `python -m unittest discover -s tests`.
</verification>

<success_criteria>
CFG-03 is satisfied for foundation code when all diagnostics can call the shared redaction helper and tests prove sensitive values are not emitted.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-project-foundation/01-02-SUMMARY.md` when done.
</output>
