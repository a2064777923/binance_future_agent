---
phase: 01-isolated-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - pyproject.toml
  - README.md
  - src/bfa/__init__.py
  - tests/__init__.py
autonomous: true
requirements:
  - ISO-01
  - ISO-02
requirements_addressed:
  - ISO-01
  - ISO-02
must_haves:
  truths:
    - "The repository at F:\\binance_futures_agent is an independent Python project with no source dependency on the stock project path per D-01."
    - "Runtime data, logs, local databases, exports, env files, and credential files are excluded from git per ISO-02."
    - "A developer can install the package locally and run the initial test command per D-02."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project metadata, package discovery, and unittest-compatible dev workflow"
    - path: ".gitignore"
      provides: "Secret, runtime, log, database, and export exclusions"
    - path: "README.md"
      provides: "Local setup and verification workflow"
    - path: "src/bfa/__init__.py"
      provides: "Importable package root"
    - path: "tests/__init__.py"
      provides: "Discoverable unittest package"
  key_links:
    - from: "pyproject.toml"
      to: "src/bfa/__init__.py"
      via: "src package layout"
      pattern: "package-dir.*src"
    - from: ".gitignore"
      to: "runtime filesystem"
      via: "git exclusions for data, runtime, logs, local databases, exports, and env files"
      pattern: "(data/|runtime/|logs/|\\.env)"
---

<objective>
Create the independent Python project scaffold and local developer workflow for Phase 1.

Purpose: Establish ISO-01 and ISO-02 before any config, secret, CLI, or trading logic is added.
Output: A `src/bfa` package skeleton, package metadata, git hygiene rules, and README workflow notes.
</objective>

<source_audit>
GOAL covered: Establish the independent repository and developer workflow.
REQ covered: ISO-01, ISO-02.
RESEARCH covered: Recommended `src/bfa` package layout, `pyproject.toml`, `.gitignore`, tests directory, and dev workflow.
CONTEXT covered: D-01 isolation and D-02 Python package layout.
Not covered in this plan: CFG-01, CFG-02, CFG-03, and ISO-03 are assigned to plans 02 through 04.
</source_audit>

<tasks>
<task type="auto">
  <name>Task 1: Create Python package scaffold</name>
  <files>pyproject.toml, src/bfa/__init__.py, tests/__init__.py</files>
  <action>Create a Python package layout per D-02 using `src/bfa` as the only importable application package. Configure `pyproject.toml` with project metadata for Binance Futures Agent, Python 3.11+ compatibility, setuptools package discovery from `src`, and a console-script entry name reserved for the CLI that plan 04 will implement. Do not add external runtime dependencies in Phase 1 unless a package legitimacy audit exists; use the standard library for this scaffold. Keep scripts as thin entry points by pointing metadata at `bfa.cli:main` without adding trading, Binance, OpenAI, or strategy logic.</action>
  <verify>
    <automated>python -m pip install -e . && python -m unittest discover -s tests</automated>
  </verify>
  <acceptance_criteria>`src/bfa` imports as a package, unittest discovery succeeds with the scaffold present, and no application file imports from outside `F:\binance_futures_agent` per D-01.</acceptance_criteria>
  <done>`src/bfa` imports as a package, unittest discovery succeeds with the scaffold present, and no application file imports from outside `F:\binance_futures_agent` per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Add repository hygiene exclusions</name>
  <files>.gitignore</files>
  <action>Create `.gitignore` rules for Python caches/build artifacts, virtual environments, `.env` files, env variants, credential/key files, local SQLite/database files, raw exports, runtime directories, logs, data directories, coverage output, and editor noise. Include `data/`, `runtime/`, and `logs/` as ignored paths because AGENTS.md assigns durable runtime data, transient state, and logs there. The rules must protect ISO-02 without ignoring source, tests, docs, `.env.example`, or planning files.</action>
  <verify>
    <automated>git check-ignore -- .env && git check-ignore -- logs/app.log && git check-ignore -- runtime/state.json && git check-ignore -- data/local.db && git check-ignore -- raw_exports/sample.csv</automated>
  </verify>
  <acceptance_criteria>Secret-bearing files and generated runtime artifacts are ignored, while `.env.example`, source files, tests, docs, and plan files remain eligible for tracking.</acceptance_criteria>
  <done>Secret-bearing files and generated runtime artifacts are ignored, while `.env.example`, source files, tests, docs, and plan files remain eligible for tracking.</done>
</task>

<task type="auto">
  <name>Task 3: Document local developer workflow</name>
  <files>README.md</files>
  <action>Write README sections for the project purpose, isolation boundary per D-01, local setup from `F:\binance_futures_agent`, editable install, default verification command `python -m unittest discover -s tests`, and `git diff --check`. State that `.env` holds local secrets and is never committed, while `.env.example` documents names only. Keep the README focused on Phase 1 workflow and explicitly exclude Binance order placement, OpenAI calls, and server deployment from this phase.</action>
  <verify>
    <automated>python -m pip install -e . && python -m unittest discover -s tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>A developer can follow the README to create a local environment, install the package, and run the test and diff hygiene commands without reading any external repository.</acceptance_criteria>
  <done>A developer can follow the README to create a local environment, install the package, and run the test and diff hygiene commands without reading any external repository.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> git repository | Local files may contain secrets or generated artifacts that must not enter version control. |
| package metadata -> import system | The package layout must avoid accidental imports from repository root or adjacent projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `pyproject.toml` package discovery | mitigate | Use `src` package discovery so imports resolve from `src/bfa` and not adjacent directories per D-02. |
| T-01-02 | Information Disclosure | `.gitignore` | mitigate | Ignore env files, credential files, runtime data, logs, local databases, and raw exports per ISO-02. |
| T-01-03 | Tampering | project isolation | mitigate | Keep setup and README instructions scoped to `F:\binance_futures_agent` per D-01. |
| T-01-SC | Tampering | package installs | accept | This plan adds no package-manager install task beyond standard editable install of the local project. |
</threat_model>

<verification>
Run `python -m pip install -e .`, `python -m unittest discover -s tests`, `git diff --check`, and `git status --short` after the scaffold is created. Confirm the only new or modified files are inside `F:\binance_futures_agent`.
</verification>

<success_criteria>
ISO-01 and ISO-02 are satisfied when the independent package scaffold exists, the developer workflow is documented, ignored paths protect secrets and runtime artifacts, and the verification commands pass.
</success_criteria>

<output>
Create `.planning/phases/01-isolated-project-foundation/01-01-SUMMARY.md` when done.
</output>
